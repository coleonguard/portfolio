title: "Cole Johnson"
description: "This is my portfolio. I hope you enjoy!"
author: "Cole Brayton Johnson"

# Remove the theme line since you are now using the theme files directly
# theme: jekyll-theme-hyde

# Other optional settings
markdown: kramdown
kramdown:
  input: GFM

baseurl: ""  # Use an empty string if the site is served from the root (e.g., a custom domain)
